package mockifer.example.android

object MockiferUtil {
    fun pushMock(mockId: String) {
        // Pushing mocks only supported in mock app
    }

    fun reset() {
        // Reset only supported in mock app
    }
}