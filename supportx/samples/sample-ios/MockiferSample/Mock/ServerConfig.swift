import Foundation

class ServerConfig {
    // We can fix the mock app server url because Mockifer will be embedded
    static let serverPort: uint = 8507
    static let serverUrl = "http://localhost"
}

